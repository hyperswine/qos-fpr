# mymod.fpr — a module: just a file. Identity = hash of this AST.

T = Type (T Int).

x = 41.

f : String -> String.
f s = "mymod.f got: {s}".

double : Int -> Int.
double n = n * 2.

mk : Int -> T.
mk n = T n.

unT : T -> Int.
unT t = case t of T v -> v.

# a comment appended after pinning
