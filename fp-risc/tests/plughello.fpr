# plughello.fpr -- the SECOND plugin, at sub-slot 1: proves multi-slot
# enumeration (tests/qsys.fpr) and that table indices follow attach
# order (apps/index order, by mkdisk.py construction).
#
#   make plugin-qa-macos PROG=tests/plughello.fpr PLUGSLOT=1

helloTitle u = "plughello: the second sub-slot".
helloDouble n = n + n.

main = "plughello is a plugin: it is enumerated, not run".
